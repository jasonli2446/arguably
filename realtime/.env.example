# Arguably Realtime SFU – Environment Variables

# Public IP address for WebRTC (use your server's public IP in production)
ANNOUNCED_IP=127.0.0.1

# HTTP + Socket.io port
LISTEN_PORT=3001

# Number of mediasoup workers (default: min(cpus, 4))
# MEDIASOUP_NUM_WORKERS=2

# RTC port range for media traffic
RTC_MIN_PORT=40000
RTC_MAX_PORT=40100

# TURN server settings
TURN_HOST=127.0.0.1
TURN_PORT=3478
TURN_USERNAME=arguably
TURN_PASSWORD=arguably-turn-password
